export enum TextFiltersValuesEnum {
  contain = 'contain',
  notContain = 'notContain',
  startsWith = 'startsWith',
  endsWith = 'endsWith',
  equals = 'equals',
  notEqual = 'notEqual',
  empty = 'empty',
  notEmpty = 'notEmpty',
  null = 'null',
  notNull = 'notNull',
}

export enum TextFiltersLabelsEnum {
  contain = 'Contains',
  notContain = 'Does Not Contain',
  startsWith = 'Starts With',
  endsWith = 'Ends With',
  equals = 'Equals',
  notEqual = 'Does Not Equal',
  empty = 'Empty',
  notEmpty = 'Not Empty',
  null = 'Null',
  notNull = 'Not Null',
}

export enum FilterOperatorEnum {
  AND = 'AND',
  OR = 'OR'
}